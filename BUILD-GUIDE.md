# دليل بناء وتوزيع التطبيق - Build & Distribution Guide
## Fikra Software POS System

---

## 📦 كيفية بناء التطبيق للتوزيع / How to Build for Distribution

### الطريقة 1: استخدام Electron Builder (موصى بها) / Using Electron Builder (Recommended)

#### الخطوات / Steps:

**1. تأكد من تثبيت جميع الحزم / Install all packages:**
```bash
npm install
```

**2. بناء التطبيق لنظام Windows:**
```bash
npm run build-win
```

هذا الأمر سينشئ:
- ملف تثبيت `.exe` في مجلد `dist`
- التطبيق جاهز للتوزيع مع أيقونة Fikra Software
- اختصار سطح المكتب وقائمة ابدأ

**3. العثور على الملف:**
بعد اكتمال البناء، ستجد الملف في:
```
My-Store-master/dist/Fikra POS Setup 0.1.0.exe
```

---

### الطريقة 2: استخدام Electron Packager

**بناء نسخة محمولة (بدون تثبيت):**
```bash
npm run package-win
```

هذا سينشئ مجلد في `release-builds` يحتوي على:
- `FikraPOS.exe` - التطبيق القابل للتشغيل
- جميع الملفات المطلوبة
- يمكنك نسخ المجلد بالكامل إلى أي كمبيوتر

---

## 🚀 كيفية توزيع التطبيق / How to Distribute

### الخيار 1: ملف التثبيت (Setup.exe)

**المميزات:**
- ✅ تثبيت احترافي
- ✅ أيقونة على سطح المكتب
- ✅ إضافة لقائمة ابدأ
- ✅ إلغاء تثبيت سهل

**كيفية الاستخدام:**
1. انسخ ملف `Fikra POS Setup 0.1.0.exe` من مجلد `dist`
2. أرسله للعميل عبر USB أو البريد الإلكتروني أو رابط تحميل
3. العميل يقوم بتشغيل الملف واتباع معالج التثبيت
4. بعد التثبيت، يمكن تشغيل التطبيق من أيقونة سطح المكتب أو قائمة ابدأ

### الخيار 2: نسخة محمولة (Portable)

**المميزات:**
- ✅ لا يحتاج تثبيت
- ✅ يعمل من أي مكان (USB, مجلد مشترك)
- ✅ سهل النقل

**كيفية الاستخدام:**
1. انسخ مجلد `FikraPOS-win32-x64` من `release-builds`
2. ضعه على USB أو ضغطه في ملف ZIP
3. العميل يفك الضغط ويشغل `FikraPOS.exe`

---

## 💻 متطلبات النظام / System Requirements

**للمستخدمين النهائيين:**
- Windows 7 أو أحدث
- 2 GB RAM (4 GB موصى به)
- 500 MB مساحة فارغة على القرص
- لا يحتاج Node.js أو أي برامج إضافية!

**للمطورين (البناء):**
- Node.js 10 أو أحدث
- npm 6 أو أحدث
- Windows 7 أو أحدث

---

## 🔧 إعدادات التطبيق / Application Settings

### تغيير الأيقونة / Changing the Icon

إذا أردت تغيير الأيقونة:
1. استبدل الملف `assets/images/icon.ico`
2. تأكد أن الملف بصيغة `.ico` (استخدم أداة مثل [ICO Convert](https://icoconvert.com/))
3. أعد البناء

### تغيير معلومات التطبيق / Changing App Info

في ملف `package.json`:
```json
{
  "name": "FikraPOS",
  "version": "0.1.0",
  "description": "وصف التطبيق هنا",
  "author": "Fikra Software"
}
```

---

## 📝 ملفات مهمة / Important Files

| الملف | الغرض |
|-------|--------|
| `start.js` | نقطة البداية للتطبيق |
| `server.js` | السيرفر المحلي (Express) |
| `index.html` | الواجهة الرئيسية |
| `assets/images/icon.ico` | أيقونة التطبيق |
| `package.json` | إعدادات المشروع والبناء |

---

## 🐛 حل المشاكل / Troubleshooting

### المشكلة: البناء يفشل

**الحل:**
```bash
# احذف المجلدات القديمة
rmdir /s /q node_modules
rmdir /s /q dist
rmdir /s /q release-builds

# أعد التثبيت
npm install

# جرب البناء مرة أخرى
npm run build-win
```

### المشكلة: التطبيق لا يعمل على كمبيوتر آخر

**الحل:**
- تأكد من تثبيت Windows Visual C++ Redistributable
- يمكن تحميله من: https://aka.ms/vs/17/release/vc_redist.x64.exe

### المشكلة: الأيقونة لا تظهر

**الحل:**
- تأكد أن ملف `.ico` موجود وصحيح
- أعد بناء التطبيق بالكامل
- في بعض الأحيان يحتاج Windows لإعادة تشغيل لتحديث الأيقونات

---

## 📤 الأوامر السريعة / Quick Commands

```bash
# للتطوير (مع إعادة تحميل تلقائية)
npm run electron

# بناء ملف تثبيت Windows
npm run build-win

# بناء نسخة محمولة
npm run package-win

# تشغيل التطبيق مباشرة
npm start
```

---

## 🌟 نصائح للتوزيع الاحترافي / Professional Distribution Tips

### 1. التوقيع الرقمي (Code Signing)
للمزيد من المصداقية، يمكنك توقيع التطبيق رقمياً:
- احصل على شهادة Code Signing من جهة موثوقة
- أضف إعدادات التوقيع في `package.json`

### 2. التحديثات التلقائية
استخدم `electron-updater` لإضافة نظام تحديثات:
```bash
npm install electron-updater
```

### 3. السيرفر أو السحابة
- ارفع ملف التثبيت على Google Drive أو Dropbox
- أو استخدم GitHub Releases للتوزيع المجاني

### 4. إنشاء صفحة تحميل
أنشئ صفحة ويب بسيطة مع:
- رابط التحميل
- متطلبات النظام
- دليل الاستخدام
- معلومات الاتصال للدعم

---

## 📋 قائمة التحقق قبل التوزيع / Pre-Distribution Checklist

- [ ] اختبر التطبيق على كمبيوتر نظيف (بدون Node.js)
- [ ] تأكد من أن جميع الميزات تعمل
- [ ] تحقق من الأيقونة وشعار التطبيق
- [ ] اختبر عملية التثبيت وإلغاء التثبيت
- [ ] أعد تشغيل الكمبيوتر واختبر مرة أخرى
- [ ] تأكد من وجود معلومات الاتصال للدعم الفني
- [ ] جهز دليل استخدام بسيط (PDF أو فيديو)

---

## 📞 الدعم / Support

**معلومات تسجيل الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin`

**العملة:** EGP (الجنيه المصري)

**اللغة:** العربية (RTL)

---

## 🎉 تهانينا!

التطبيق الآن جاهز للتوزيع كتطبيق سطح مكتب احترافي!

**Fikra Software POS** - نظام نقطة البيع الكامل 🚀

---

تم إنشاؤه بواسطة: **فكرة سوفت وير**  
التاريخ: نوفمبر 2025

